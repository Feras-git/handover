enum StateStatus {
  initial,
  loading,
  failure,
  successful,
}

enum AccountType {
  customer,
  driver,
}

enum OrderStatus {
  onTheWay,
  pickedUpDelivery,
  nearDeliveryDestination,
  deliveredPackage,
}

enum DistanceUnit {
  meters,
  kilometers,
}
